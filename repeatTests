#!/bin/bash

# always do an initial full build 1st
mvn clean install
if [ $? != 0 ]; then
  echo "Failed tests"
  exit 10
fi

while true; do
  mvn clean install -rf :tests
  if [ $? != 0 ]; then
    echo "Failed tests"
    exit 10
  fi
done

